/*
    - Führen Sie bitte zunächst einen Delegat-Typen "MeinRemoveDelegate" ein (Übergabewert: 1 String / Rückgabewert: 1 Bool) 
    - Führen Sie ferner bitte (in der Klasse Program) die folgende Methode ein:
        Name: meinRemoveAll
        Übergabewerte: eine String-Liste und ein Delegat vom Typ "MeinRemoveDelegate"
        Funktion: a) Löscht alle Elemente der Liste, für die der Rückgabewert der Funktion, auf den das Delegat verweist, "true" lautet 
                  b) Zählt die Anzahl der Elemente, die gelöscht werden
                  c) OHNE die von C# vorgegebene RemoveAll-Methode lösen
        Rückgabewert: Die ermittelte Anzahl (der gelöschten Elemente)

    Im Main:
       - Führen Sie bitte zunächst eine String-Liste "SL" mit folgenden Elementen ein: 
          { "Tee", "Auto", "Beine", "Abrakadabra", "C# ist nicht die beste Programmiersprache der Welt ..." }
       - Lassen Sie anschließend bitte zur Kontrolle den aktuellen Inhalt von SL auf der Konsole ausgeben
       - Rufen Sie bitte daraufhin die Methode  "meinRemoveAll()"  auf
           + übergeben Sie hierbei bitte die beiden folgenden Parameter:
                a) Die String-Liste
                b) Eine Lamda-Expression (vom Typ "MeinRemoveDelegate") die GENAU DANN "true" zurückgibt WENN die Länge des Strings >5 ist
           + speichern Sie bitte den Rückgabewert von "meinRemoveAll()"
        - Lassen Sie abschließend bitte auf der Konsole ausgeben:
            a) alle (verbliebenen) Elemente der String-Liste
            b) die Anzahl der gelöschten Elemente
*/